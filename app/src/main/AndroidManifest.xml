<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.rahma.proyekrahma">
<!--Pada kode program ini digunakan untuk menampilkan nama label/nama aplikasi yang telah kita buat dengan
beberapa kategory dan melakukan sebuah aksi yang aktivitas berhubungan/tersambung dengan aktiviti-aktivity lainnya.-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyekRahma">
        <activity
            android:name=".Splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".About.AboutActivity"
            android:exported="true" />
        <activity
            android:name=".Detail.DetailActivity"
            android:exported="true" />
        <activity
            android:name=".Main.MainActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>